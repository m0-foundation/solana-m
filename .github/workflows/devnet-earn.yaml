name: Devnet Build and Deploy - Earn

on:
  push:
    branches:
      - main
    paths:
      - 'programs/earn/**'

jobs:
  deploy-earn:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Environment
        uses: ./.github/setup

      - name: Write devkey
        run: echo "${{ secrets.DEVNET_DEPLOYER_KEYPAIR }}" > devnet-keypair.json
        shell: bash

      - name: Upgrade
        run: make upgrade-earn-devnet
        shell: bash
