/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beet from '@metaplex-foundation/beet'
import * as beetSolana from '@metaplex-foundation/beet-solana'

/**
 * Arguments used to create {@link Earner}
 * @category Accounts
 * @category generated
 */
export type EarnerArgs = {
  earnManager: beet.COption<web3.PublicKey>
  recipientTokenAccount: beet.COption<web3.PublicKey>
  lastClaimIndex: beet.bignum
  lastClaimTimestamp: beet.bignum
  isEarning: boolean
  bump: number
  user: web3.PublicKey
  userTokenAccount: web3.PublicKey
}

export const earnerDiscriminator = [236, 126, 51, 96, 46, 225, 103, 207]
/**
 * Holds the data for the {@link Earner} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Earner implements EarnerArgs {
  private constructor(
    readonly earnManager: beet.COption<web3.PublicKey>,
    readonly recipientTokenAccount: beet.COption<web3.PublicKey>,
    readonly lastClaimIndex: beet.bignum,
    readonly lastClaimTimestamp: beet.bignum,
    readonly isEarning: boolean,
    readonly bump: number,
    readonly user: web3.PublicKey,
    readonly userTokenAccount: web3.PublicKey
  ) {}

  /**
   * Creates a {@link Earner} instance from the provided args.
   */
  static fromArgs(args: EarnerArgs) {
    return new Earner(
      args.earnManager,
      args.recipientTokenAccount,
      args.lastClaimIndex,
      args.lastClaimTimestamp,
      args.isEarning,
      args.bump,
      args.user,
      args.userTokenAccount
    )
  }

  /**
   * Deserializes the {@link Earner} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [Earner, number] {
    return Earner.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Earner} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<Earner> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find Earner account at ${address}`)
    }
    return Earner.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'MzeRokYa9o1ZikH6XHRiSS5nD8mNjZyHpLCBRTBSY4c'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, earnerBeet)
  }

  /**
   * Deserializes the {@link Earner} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Earner, number] {
    return earnerBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link Earner} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return earnerBeet.serialize({
      accountDiscriminator: earnerDiscriminator,
      ...this,
    })
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Earner} for the provided args.
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   */
  static byteSize(args: EarnerArgs) {
    const instance = Earner.fromArgs(args)
    return earnerBeet.toFixedFromValue({
      accountDiscriminator: earnerDiscriminator,
      ...instance,
    }).byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Earner} data from rent
   *
   * @param args need to be provided since the byte size for this account
   * depends on them
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    args: EarnerArgs,
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      Earner.byteSize(args),
      commitment
    )
  }

  /**
   * Returns a readable version of {@link Earner} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      earnManager: this.earnManager,
      recipientTokenAccount: this.recipientTokenAccount,
      lastClaimIndex: (() => {
        const x = <{ toNumber: () => number }>this.lastClaimIndex
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      lastClaimTimestamp: (() => {
        const x = <{ toNumber: () => number }>this.lastClaimTimestamp
        if (typeof x.toNumber === 'function') {
          try {
            return x.toNumber()
          } catch (_) {
            return x
          }
        }
        return x
      })(),
      isEarning: this.isEarning,
      bump: this.bump,
      user: this.user.toBase58(),
      userTokenAccount: this.userTokenAccount.toBase58(),
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const earnerBeet = new beet.FixableBeetStruct<
  Earner,
  EarnerArgs & {
    accountDiscriminator: number[] /* size: 8 */
  }
>(
  [
    ['accountDiscriminator', beet.uniformFixedSizeArray(beet.u8, 8)],
    ['earnManager', beet.coption(beetSolana.publicKey)],
    ['recipientTokenAccount', beet.coption(beetSolana.publicKey)],
    ['lastClaimIndex', beet.u64],
    ['lastClaimTimestamp', beet.u64],
    ['isEarning', beet.bool],
    ['bump', beet.u8],
    ['user', beetSolana.publicKey],
    ['userTokenAccount', beetSolana.publicKey],
  ],
  Earner.fromArgs,
  'Earner'
)
